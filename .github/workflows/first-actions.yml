name: firstworkflow
on: workflow_dispatch
jobs: 
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello world1!"
            echo "Hello world2!"
      - name: Print Goodbye
        run: echo "Done - bye!"
       
